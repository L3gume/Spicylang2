[package]
name = "spicylang2"
version = "0.1.0"
edition = "2024"

[dependencies]
lalrpop-util = "0.23.1"
# MLIR bindings for codegen (see src/codegen.rs); uncomment to enable the
# MLIR -> LLVM backend + JIT pipeline. Requires a system LLVM with llvm-config
# and the MLIR libraries.
# melior = "0.27.4"

[build-dependencies]
lalrpop = "0.23.1"

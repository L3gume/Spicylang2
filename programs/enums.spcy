
enum Option('a) =
    Some('a)
    | None;

enum Result('a, 'b) =
    Ok('a)
    | Err('b);

let get_opt = \x => Some x;

let get_res = \opt => match opt
    | Some val  => Ok val
    | None      => Err "No value found"
    ;

